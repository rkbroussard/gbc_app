<%= direct_upload_form_for @bulletin do |f| %>
  <div class="well">
  	<%= f.label :attachment %>
  	<%= f.file_field :attachment %>
  <%= f.submit "Save", class: "btn btn-large btn-primary" %>
  </div>
<% end %>